# 车险理赔预测系统 — 系统架构与代码详解

> 本文档面向答辩准备，逐文件讲解系统实现、各层职责与调用关系。

---

## 一、系统总览

### 1.1 三层架构

```
浏览器 (Vue 3 + Element Plus + ECharts)
    │  HTTP :5173
    ▼
Spring Boot (Java, 端口 9090)  ←→  MySQL 数据库 (gra_data)
    │  HTTP 代理 ML 请求
    ▼
FastAPI (Python, 端口 8000)  →  PyTorch MLP 神经网络模型
```

三个进程必须同时运行。Spring Boot 是整个系统的 **API 网关**，它既直接处理 CRUD 业务，也将 ML 相关的预测/训练请求转发给 Python 的 FastAPI 服务。

### 1.2 技术栈速查

| 层 | 技术 | 作用 |
|---|---|---|
| 前端 | Vue 3 + Vite + Element Plus + ECharts + Axios | 页面渲染、用户交互、图表展示 |
| 后端 | Spring Boot + MyBatis + PageHelper + Hutool | REST API、业务逻辑、数据库访问 |
| ML | FastAPI + PyTorch + scikit-learn + matplotlib | 神经网络推理、模型训练、可解释性分析 |
| 数据库 | MySQL (gra_data) | 存储保单、理赔、车辆、用户、预测结果等数据 |
| 认证 | HttpSession | 基于 Session 的登录认证与角色鉴权 |

---

## 二、数据库设计

### 2.1 核心业务表（三表通过 ID 一对一关联）

```
motor_insurance (保单主表)
    ├── ID (主键, 同时也是 claim_record 和 vehicle_info 的外键)
    ├── 保单维度字段: Date_start_contract, Date_last_renewal, Premium, Payment...
    ├── 被保人维度: Date_birth, Date_driving_licence, Seniority...
    ├── 车辆引用字段: Type_risk, Area, Second_driver
    └── creator_employee_no (创建者工号, 用于数据权限隔离)

claim_record (理赔记录表)
    ├── ID (主键 + 外键 → motor_insurance.ID)
    ├── Cost_claims_year (年度理赔成本)
    ├── N_claims_year (年度理赔次数, 也是 ML 的训练标签)
    ├── N_claims_history, R_Claims_history (历史理赔统计)
    ├── Type_risk, Area
    └── creator_employee_no

vehicle_info (车辆信息表)
    ├── ID (主键 + 外键 → motor_insurance.ID)
    ├── Year_matriculation, Power, Cylinder_capacity, Value_vehicle
    ├── N_doors, Type_fuel, Length, Weight
    ├── Type_risk
    └── creator_employee_no
```

**设计要点：**
- 三张表通过同一个 `ID` 值进行一对一关联，这个 ID 即"保单编号"
- 保单表中不直接存车辆具体参数和理赔金额，而是拆分到另外两张表
- 这种设计使得 ML 预测时需要 JOIN 三张表来拼出完整特征向量

### 2.2 其他重要表

| 表名 | 用途 |
|---|---|
| `sys_user` | 用户表 (employee_no, password(MD5), role: ADMIN/USER) |
| `insur_pred` | 预测结果表 (claim_probability, risk_level, model_version, 解释因子JSON) |
| `train_data` | 训练数据宽表 (三表 JOIN 后的结果, 供 ML 训练使用) |
| `motor_insurance_backup_20260413_idfix` | 保单备份表 (train_data 表结构的模板) |
| `motor_insurance_id_sequence` | 自增序列表 (用于生成新的保单 ID) |

---

## 三、后端代码详解 (Spring Boot)

### 3.1 项目入口与配置

#### `SpringbootApplication.java`
```java
@SpringBootApplication
@MapperScan("com.example.mapper")  // 扫描 MyBatis Mapper 接口
public class SpringbootApplication {
    public static void main(String[] args) {
        SpringApplication.run(SpringbootApplication.class, args);
    }
}
```
- `@MapperScan` 告诉 MyBatis 去哪里找 Mapper 接口
- 一个注解 `@SpringBootApplication` 就完成了自动配置、组件扫描

#### `application.yml` — 核心配置
```yaml
server.port: 9090                          # Spring Boot 端口
spring.datasource: mysql://localhost:3306/gra_data  # 数据库连接
mybatis.configuration.map-underscore-to-camel-case: true  # 下划线→驼峰自动映射
fastapi.base-url: http://localhost:8000     # FastAPI 地址
fastapi.timeout: 30000                      # 调用 FastAPI 的超时时间
```

---

### 3.2 common 包 — 基础设施

#### `Result.java` — 统一响应格式

**作用：** 所有后端接口返回的数据都包装成统一结构。

```java
public class Result {
    private String code;    // "200" 成功, "500" 失败, "401" 未登录
    private String msg;     // 提示信息
    private Object data;    // 实际数据
}
```

前端 Axios 拦截器会检查 `res.code === '401'` 来跳转登录页。这个类是前后端通信的"协议"。

#### `RoleEnum.java` — 角色枚举

```java
public enum RoleEnum { ADMIN, STUDENT }
```

注意：虽然叫 STUDENT，但实际在系统中代表"普通操作员用户"。数据库中存储的角色值是 `"ADMIN"` 和 `"USER"`。

#### `SessionUserUtil.java` — 会话与权限工具类

**最核心的权限控制类**，提供四个静态方法：

| 方法 | 功能 | 使用场景 |
|---|---|---|
| `requireLogin(session)` | 从 Session 取当前用户，未登录则抛异常 | 所有需要登录的接口 |
| `requireAdmin(session)` | 要求管理员角色，否则抛异常 | 模型训练、用户管理 |
| `resolveDataScopeEmployeeNo(session)` | 管理员返回 null（看全部），普通用户返回自己的工号（只看自己的） | 列表查询的数据权限隔离 |
| `requireOwnerOrAdmin(creatorId, session)` | 只有数据创建者或管理员才能操作 | 编辑/删除操作 |

**数据权限逻辑：** 管理员能看到所有数据，普通用户只能看到自己创建的保单/理赔/车辆记录。

#### `CorsConfig.java` — 跨域配置

允许前端 (localhost:5173) 跨域访问后端 (localhost:9090)。设置了 `allowCredentials(true)` 以支持携带 Session Cookie。

#### `DateUtil.java` — 日期格式转换工具

系统内部日期在两种格式间转换：
- **数据库存储格式：** `dd/MM/yyyy`（如 01/01/2019）
- **前端展示格式：** `yyyy/MM/dd`（如 2019/01/01）

同时支持模糊查询：用户输入 `2019` 可以匹配该年所有记录。

---

### 3.3 exception 包 — 异常处理

#### `CustomException.java` — 自定义业务异常

继承 `RuntimeException`，携带 `msg` 属性。Service 层遇到业务错误（如"保单不存在"、"密码错误"）时抛出。

#### `GlobalExceptionHandler.java` — 全局异常拦截

使用 `@ControllerAdvice` 拦截所有 Controller 抛出的异常：
- `CustomException` → 返回 `Result.error(e.getMsg())`
- `DataIntegrityViolationException` → 返回"数据不完整或格式不正确"
- 其他 `Exception` → 返回通用"请求失败"

这样 Controller 代码就不需要 try-catch，异常会自动转为友好的 JSON 响应。

---

### 3.4 entity 包 — 数据实体

共 7 个实体类（加上几个请求体类），每个对应一张数据库表：

| 实体类 | 对应表 | 关键字段 |
|---|---|---|
| `User` | sys_user | employeeNo, password, role, homeShortcuts |
| `MotorInsurance` | motor_insurance | 26 个字段，涵盖保单、被保人、部分车辆信息 |
| `ClaimTypes` | claim_record | costClaimsYear, nClaimsYear, nClaimsHistory, rClaimsHistory 等 |
| `VehicleInfo` | vehicle_info | yearMatriculation, power, cylinderCapacity, valueVehicle 等 |
| `InsurPred` | insur_pred | claimProbability, riskLevel, modelVersion, positiveFactors, negativeFactors |

**特殊设计的实体：**
- `MotorInsurance` 使用 `@JsonProperty("nClaimsYear")` 等注解处理 Java 驼峰命名与 JSON 字段的映射
- `InsurPred` 有两个 factor JSON 字段：`positiveFactorsJson` / `negativeFactorsJson`（数据库存储），对应内存中的 `List<Map>` 对象（标记了 `@JsonIgnore` 在 getter 上，避免重复序列化）
- `PredictionRequest` / `PolicyIdPredictionRequest` / `TrainDataImportRequest` 等是请求体 DTO，不映射数据库表

---

### 3.5 mapper 包 — 数据访问层 (MyBatis)

Mapper 接口使用 **注解式 SQL**（少数复杂查询用 XML），MyBatis 自动生成实现类。

#### `UserMapper.java`
- `createTableIfNotExists()` — DDL：自动建表
- `initAdmin(User user)` — 使用 `INSERT ... WHERE NOT EXISTS` 实现幂等初始化管理员
- 标准 CRUD：insert, selectById, selectByEmployeeNo, updateById, deleteById
- 密码使用 MD5 存储（`SecureUtil.md5()`）

#### `MotorInsuranceMapper.java`
- 标准 CRUD + 分页查询（`selectAll` 配合 PageHelper）
- 统计查询：`statisticsByRiskType`, `statisticsByArea`, `statisticsByPayment` 等
- `overallStatistics` — 计算总保费、赔付率、2018年保单数等汇总指标

#### `ClaimTypesMapper.java` / `VehicleInfoMapper.java`
- 与 MotorInsuranceMapper 结构类似
- 统计方法按风险类型、地区/燃料类型等维度分组

#### `InsurPredMapper.java`
- `insert` — 插入预测结果，使用 `@Options(useGeneratedKeys = true)` 自动回填自增主键 pred_id
- `selectAll` — 对应 XML 中的动态 SQL，支持多条件筛选
- `riskLevelDistribution` — 按风险等级分组统计
- `claimProbabilityHistogram` — 按概率区间（0.1 为步长）分组，用于前端直方图
- `overallStatistics` — 预测总数、平均概率、正向预测率

---

### 3.6 service 包 — 业务逻辑层

#### `UserService.java` — 用户与认证服务

**核心功能：**
- **注册 (`register`)：** 验证工号（6 位数字）、姓名、密码、注册口令（admin 用 "add123"，普通用户用 "add"），创建用户后自动登录
- **登录 (`login`)：** 验证工号+密码（MD5），将用户信息存入 Session，返回上次登录时间
- **退出 (`logout`)：** 销毁 Session
- **首页数据：** `getHomeShortcuts` 返回用户的快捷入口配置（最多 4 个），`getHomeTodos` 返回当前用户缺失理赔记录/车辆信息的保单列表
- **用户管理 (admin only)：** 增删改查用户，不能删除自己、最后一个管理员

**初始化逻辑 (`@PostConstruct init`)：**
1. 自动建表 `sys_user`（如果不存在）
2. 检查并添加 `home_shortcuts` 列（兼容旧表结构）
3. 幂等插入默认管理员（工号 000001，密码 admin123）

#### `MotorInsuranceService.java` — 保单管理服务

**核心设计：**
- **新增保单：** 系统自动分配 ID（通过 `motor_insurance_id_sequence` 表获取自增序列号），而不是用户手动指定。创建者工号自动记录
- **日期格式转换：** 前端传 `yyyy/MM/dd` → 转成 `dd/MM/yyyy` 存库，读取时反向转换
- **日期约束校验：** 续保日期不能早于合同开始日期，驾照签发必须晚于出生日期+18年
- **数据权限：** 通过 `SessionUserUtil` 实现，管理员看全部，普通用户只看自己的
- **模糊查询：** 日期字段支持年份/年月模糊匹配

#### `VehicleInfoService.java` / `ClaimTypesService.java` — 车辆信息与理赔记录服务

- 结构与 MotorInsuranceService 类似
- **关键约束：** 新增车辆信息/理赔记录时，必须先存在对应 ID 的保单记录（外键约束）
- `typeRisk` 和 `area` 等字段从保单表自动同步
- 创建者从保单继承

#### `InsurPredService.java` — 预测服务（核心）

**两种预测方式：**

1. **手工预测 (`predictAndSave`)：**
   - 用户在前端填写 26 个特征字段
   - 验证所有必填字段完整
   - 将 MotorInsurance 对象序列化为 JSON
   - 通过 Hutool `HttpRequest` POST 到 FastAPI 的 `/predict` 接口
   - 将返回的预测结果（概率、风险等级、解释因子）存入 `insur_pred` 表
   - 返回带解释信息的完整预测结果

2. **基于已有保单预测 (`predictAndSaveByPolicyId`)：**
   - 根据保单 ID 查询三张表（motor_insurance + claim_record + vehicle_info）
   - 拼接成完整的特征记录
   - 后续流程与手工预测相同

**模型版本管理 (`listModelVersions`)：**
- 调用 FastAPI 的 `/models/versions` 获取可用模型版本列表
- 用户可以切换不同训练出的模型进行预测

#### `ModelTrainingService.java` — 模型训练服务

**训练数据管理：**
- `ensureTrainDataTableInitialized()` — 检查 `train_data` 表是否存在，不存在则从备份表创建
- `importTrainData(contractYear)` — 从三张业务表 JOIN 出完整训练数据，写入 `train_data`
- `syncTrainDataCsv()` — 将 `train_data` 表导出为 CSV 文件（`MLP/DataSet/train_data.csv`），FastAPI 读取此文件进行训练

**训练任务代理：**
- 所有训练相关接口（`/training/start`, `/training/jobs/latest` 等）都需要 admin 权限
- 实际训练逻辑在 FastAPI，Spring Boot 只做 HTTP 转发
- 训练图像（loss/AUC 曲线）通过 `/training/jobs/{jobId}/figures/{figureKey}` 获取，返回 `ResponseEntity<byte[]>` 即 PNG 图片二进制数据

#### `AnalyticsService.java` — 数据分析服务

**灵活的聚合查询引擎：**
- 支持两个分析主题：`policy`（保单分析）和 `claim`（理赔分析）
- 支持 8 个分组维度：typeRisk, area, distributionChannel, payment, contractStartDate, typeFuel, yearMatriculation, secondDriver
- 支持 8 个统计指标：保单数、总保费、平均保费、理赔记录数、总理赔成本、平均理赔成本、理赔频率、赔付率
- 使用 `JdbcTemplate` 动态拼接 SQL，支持多条件筛选（年份、风险类型、地区、渠道等）
- 返回汇总值 + TopN 分组排名

---

### 3.7 controller 包 — 接口层

Controller 层非常薄，只做三件事：
1. 接收 HTTP 请求参数
2. 调用对应的 Service 方法
3. 用 `Result.success(data)` 包装返回

共 8 个 Controller，URL 前缀与功能对应：

| Controller | URL 前缀 | 主要功能 |
|---|---|---|
| `AuthController` | `/auth` | 注册、登录、退出 |
| `MotorInsuranceController` | `/motorInsurance` | 保单 CRUD + 7 种统计图表数据 |
| `ClaimTypesController` | `/claimTypes` | 理赔记录 CRUD + 2 种统计 |
| `VehicleInfoController` | `/vehicleInfo` | 车辆信息 CRUD + 3 种统计 |
| `InsurPredController` | `/insurPred` | 预测（手工/基于保单）、预测结果查询、预测统计 |
| `ModelTrainingController` | `/modelTraining` | 训练启动、任务状态查询、权重保存、图像获取、训练数据导入（均需 admin） |
| `AnalyticsController` | `/analytics` | 通用数据分析查询（POST，传 JSON 指定维度和指标） |
| `UserController` | `/user` | 用户管理（admin）、首页快捷入口、待办事项 |
| `FileController` | `/files` | 文件上传下载 |

---

## 四、前端代码详解 (Vue 3)

### 4.1 核心工具文件

#### `utils/request.js` — Axios 实例

**请求拦截器：** 统一设置 `Content-Type: application/json`

**响应拦截器：**
```javascript
if (res.code === '401') {
    ElMessage.error(res.msg);
    clearCurrentUser()     // 清除 sessionStorage
    router.push("/login")  // 跳转登录页
}
```
这就是"未登录自动跳转"的实现机制。后端 `SessionUserUtil.requireLogin()` 抛出异常被 `GlobalExceptionHandler` 转为 `code='401'` 的 `Result`，前端拦截器检测到就跳转。

#### `utils/auth.js` — 用户状态管理

将用户对象存储在 `sessionStorage` 中（关闭浏览器即清除），提供 `getCurrentUser` / `setCurrentUser` / `clearCurrentUser` 三个方法。

#### `router/index.js` — 路由配置与权限守卫

**路由结构：**
```
/login          → Login.vue
/manager        → MainLayout.vue (侧边栏布局)
  /home         → Home.vue (首页仪表盘)
  /motorInsurance         → MotorInsurance.vue
  /motorInsuranceStatistics → BusinessAnalytics.vue (analyticsSubject='policy')
  /claimTypes             → ClaimTypes.vue
  /claimStatistics        → BusinessAnalytics.vue (analyticsSubject='claim')
  /vehicleInfo            → VehicleInfo.vue
  /predictionManage       → PredictionManage.vue
  /predictionStatistics   → PredictionStatistics.vue
  /modelTraining          → ModelTraining.vue (adminOnly)
  /userManage             → UserManage.vue (adminOnly)
```

**路由守卫 (`beforeEach`)：**
1. 未登录 → 强制跳转 `/login`
2. 已登录访问 `/login` → 跳转首页
3. 访问 `adminOnly` 页面但角色不是 ADMIN → 跳转首页

**组件复用：** `BusinessAnalytics.vue` 通过路由 `meta.analyticsSubject` 区分是保单分析还是理赔分析。

### 4.2 视图页面

| 页面组件 | 功能描述 |
|---|---|
| `Login.vue` | 登录/注册双 Tab 页，6 位工号 + 密码 |
| `MainLayout.vue` | 侧边栏 + 顶栏 + `<router-view>` 布局 |
| `Home.vue` | 快捷入口（可自定义最多4个）、待办列表（缺理赔/车辆信息的保单） |
| `MotorInsurance.vue` | 保单表格（分页+搜索+CRUD 弹窗） |
| `ClaimTypes.vue` | 理赔记录表格 |
| `VehicleInfo.vue` | 车辆信息表格 |
| `PredictionManage.vue` | 预测核心页：模型版本选择 + 表单填写/选择已有保单 → 展示预测结果和解释因子 |
| `PredictionStatistics.vue` | 预测结果统计：风险等级分布、概率直方图、预测列表 |
| `ModelTraining.vue` | 训练参数配置 + 启动训练 + 实时进度（轮询） |
| `ModelTrainingResult.vue` | 训练完成后的详细结果：loss/AUC 曲线图、混淆矩阵、指标汇总 |
| `BusinessAnalytics.vue` | 通用数据分析：选择主题/维度/指标 → 表格 + ECharts 图表 |
| `UserManage.vue` | 用户列表 + 新增/编辑弹窗（admin only） |

---

## 五、ML 服务代码详解 (FastAPI + PyTorch)

### 5.1 文件总览

```
MLP/
├── FastAPIApp.py        # FastAPI 应用入口, REST API 定义
├── Model.py             # MLP 神经网络模型定义 (InsuranceMLP)
├── TrainModel.py        # 训练流程: train/evaluate/checkpoint/图表生成
├── TrainingManager.py   # 训练任务管理器: 后台线程、状态持久化
├── TrainConfig.py       # 训练配置中心 (dataclass, 所有超参数)
├── DataLoader.py        # 数据加载、特征工程、预处理管线
├── DatabaseUtils.py     # 数据库/CSV 数据读取工具
├── InferenceService.py  # 推理服务: 加载模型→推理→可解释性分析
└── ablation/            # 消融实验 (模型的对比实验)
```

### 5.2 模型架构 (`Model.py`)

#### 网络结构

```
输入层 (26维特征向量)
    │
    ▼
Input Projection (Linear → LayerNorm → GELU → Dropout)
    │  input_dim → hidden_dims[0]
    ▼
ResidualBlock × N  (每个: FC→LN→GELU→Dropout→FC→LN→残差连接→GELU)
    │  各层维度: 256→512→512→256→128  (可配置)
    ▼
ClassificationHead (Linear→LN→GELU→Dropout→Linear→1)
    │  backbone_output_dim → head_hidden_dim(32) → 1
    ▼
Sigmoid → 理赔概率 [0, 1]
```

**关键设计决策：**
- **残差连接 (ResidualBlock)：** 缓解深层网络的梯度消失问题
- **LayerNorm（而非 BatchNorm）：** 对小批次更稳定
- **GELU 激活函数：** 比 ReLU 更平滑，在 NLP 和推荐系统中表现更好
- **10 次 Head 采样平均 (head_samples=10)：** 推理时对分类头进行多次前向传播取平均，类似 Monte Carlo Dropout，增加预测稳定性
- **Kaiming 初始化：** 适配 ReLU 系激活函数的权重初始化方法

#### 损失函数 `ClaimClassificationLoss`

- 使用 `BCEWithLogitsLoss`（二元交叉熵 + Logits，数值更稳定）
- **正样本权重 (pos_weight=3.10)：** 补偿正负样本不平衡。实际训练时如果配置值 ≤0，会自动计算为 `负样本数/正样本数`
- **标签平滑 (label_smoothing=0.05)：** 防止模型过自信，提升泛化能力

### 5.3 特征工程 (`DataLoader.py`)

#### 特征工程策略

**核心思路：** 将所有日期字段转换为以"最近续保日期"为观测点的相对时间差。

| 原始字段 | 转换后特征 | 转换方式 |
|---|---|---|
| Date_birth | 被保人年龄 (insured_age_years) | (观测日期 - 出生日期) / 365.25 |
| Date_driving_licence | 驾驶经验 (driving_experience_years) | (观测日期 - 驾照日期) / 365.25 |
| Date_start_contract | 合同持续时长 (contract_duration_years) | (观测日期 - 合同开始日期) / 365.25 |
| Date_next_renewal | 距下次续保天数 (days_diff) | 下次续保日期 - 观测日期 |
| Year_matriculation | 车龄 (vehicle_age_years) | 观测年份 - 注册年份 |

**其他处理：**
- 分类变量编码：Type_fuel (P→0, D→1), Distribution_channel 等直接作为数值
- 缺失值填充：中位数填充数值字段，众数填充分类字段
- 极端值截断：Premium, Value_vehicle 等字段按 99.9% 分位数截断
- StandardScaler 标准化：训练集 fit，验证集/测试集 transform

#### 数据划分

```
全量数据
├── 训练集 (train): 75% (默认) — 用于梯度更新
├── 验证集 (val):   15% — 用于早停、阈值搜索
└── 测试集 (test):  10% — 用于最终评估
```

分层抽样 (`stratify`) 保证三个集合的正负样本比例一致。

### 5.4 训练流程 (`TrainModel.py`)

#### 每个 Epoch 做什么

1. **训练阶段 (`train_one_epoch`)：** 前向传播 → 计算 loss → 反向传播 → 梯度裁剪 → 优化器更新（支持混合精度 AMP）
2. **验证阶段 (`evaluate`)：** `torch.no_grad()` 模式下计算 loss、AUC、PR-AUC、Accuracy、F1 等
3. **阈值搜索 (`search_best_threshold`)：** 在 0.05~0.95 之间找使验证集 F1 最大的阈值（可设 min_recall 约束）
4. **保存最佳模型：** 监控指标改善时保存 checkpoint
5. **早停检查：** patience=5 个 epoch 无改善则停止

#### 训练输出

- `best_model.pth` — 最佳模型权重 + 配置
- `best_threshold.pt` — 最佳分类阈值
- `scaler.pkl` — StandardScaler 参数
- `preprocess_reference.pkl` — 预处理参考信息（特征列名、填充值等）
- `figures/` — Loss 曲线、Accuracy 曲线、PR-AUC 曲线、混淆矩阵的 PNG 图

### 5.5 推理服务 (`InferenceService.py`)

#### 启动加载流程

1. 加载 `preprocess_reference.pkl`（或从 train_data.csv 重新构建）
2. 发现 `saved_weights/` 目录下的模型版本
3. 加载默认模型 + scaler + 阈值
4. 设置 `ready = True`

#### 单条预测流程 (`predict_single`)

```
用户输入 record (字典, 26个原始字段, 驼峰命名)
    │
    ▼ normalize_record: 驼峰 → 数据库原始列名
    │
    ▼ preprocess_for_inference: 
    │   日期特征工程 → 缺失值填充 → 极端值截断 → StandardScaler
    │
    ▼ model.forward: 
    │   输入层 → 残差块 → 分类头 → logit → sigmoid → 概率
    │
    ▼ 结果组装:
    │   claimProbability, claimFlag (概率≥阈值 → 1), riskLevel
    │
    ▼ 可解释性分析 (_build_local_explanation):
       逐个特征替换为训练集中位数 → 重算概率 → 计算概率变化 Δ
       正向影响 TOP3 + 负向影响 TOP2 → 生成中文解释文本
```

#### 可解释性方法

采用的是 **单特征消融 (Single Feature Ablation)** 方法：
1. 对每个有值的特征，将其替换为训练样本的中位数（或众数）
2. 用模型重新预测，计算概率变化 Δ
3. Δ > 0 说明该特征的当前值使理赔概率**上升**（风险因素）
4. Δ < 0 说明该特征的当前值使理赔概率**下降**（保护因素）
5. 选 TOP3 风险因素和 TOP2 保护因素返回

这种方法的优点是直观易懂，缺点是计算量大（26 个特征需要 26 次额外推理）。

#### 风险等级划分

```python
claimProbability < 0.30  →  LOW   (低风险)
0.30 ≤ claimProbability < 0.60  →  MEDIUM (中风险)
claimProbability ≥ 0.60  →  HIGH  (高风险)
```

### 5.6 训练任务管理 (`TrainingManager.py`)

- **单任务限制：** 同一时间只允许一个训练任务运行
- **后台线程：** 训练在独立线程中执行，不阻塞 API 响应
- **状态持久化：** 每个任务的状态保存到 `job_state.json`
- **进度回调：** 每个 epoch 结束后回调更新任务状态，前端轮询获取
- **服务重启恢复：** 启动时检查未完成的任务，标记为 "failed"
- **权重保存：** 训练完成后可将 best/last checkpoint 复制到 `saved_weights/` 目录（带 JSON 元数据）

### 5.7 FastAPI 接口汇总

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/health` | 健康检查，返回模型状态和设备信息 |
| GET | `/contract` | 返回服务的完整 API 契约文档 |
| POST | `/predict` | 单条预测（含可解释性） |
| GET | `/models/versions` | 列出所有可用模型版本 |
| POST | `/training/start` | 启动训练任务 |
| GET | `/training/jobs/latest` | 获取最近训练任务状态 |
| GET | `/training/jobs/{jobId}` | 获取指定训练任务详情 |
| GET | `/training/jobs/{jobId}/figures/{figureKey}` | 获取训练图像 PNG |
| POST | `/training/jobs/{jobId}/save-weights` | 保存模型权重 |
| POST | `/training/jobs/{jobId}/discard` | 丢弃训练任务 |

---

## 六、完整数据流追踪

以"用户基于已有保单进行理赔预测"为例：

```
 1. 用户点击"预测已有保单"按钮，选择保单 ID=1
       │
 2. Vue → POST /insurPred/predictById  { id: 1, modelVersion: "v1" }
       │  [Axios 自动携带 Cookie → Session]
       ▼
 3. InsurPredController.predictById()
       │  调用 insurPredService.predictAndSaveByPolicyId(1, "v1", session)
       ▼
 4. InsurPredService.predictAndSaveByPolicyId()
       │  ├── motorInsuranceMapper.selectById(1)    → 保单数据
       │  ├── claimTypesMapper.selectById(1)         → 理赔数据
       │  ├── vehicleInfoMapper.selectById(1)        → 车辆数据
       │  ├── 权限校验: SessionUserUtil.requireOwnerOrAdmin()
       │  └── 拼接成完整的 MotorInsurance 特征记录
       ▼
 5. callFastApiPredict(record, "v1")
       │  POST http://localhost:8000/predict
       │  Body: { record: {...26个字段}, modelVersion: "v1" }
       ▼
 6. FastAPI → InferenceService.predict_single()
       │  ├── 特征工程: 日期→相对时间差, 缺失值填充, 标准化
       │  ├── model.forward() → logit → sigmoid → 概率
       │  ├── 可解释性: 逐个特征消融 → 概率变化 Δ
       │  └── 返回: { claimProbability, riskLevel, positiveFactors, negativeFactors... }
       ▼
 7. InsurPredService.convertToInsurPred()
       │  ├── 将 JSON 转为 InsurPred 实体
       │  ├── insurPredMapper.insert(insurPred)  → 存入 insur_pred 表
       │  └── 返回带完整解释信息的 InsurPred 对象
       ▼
 8. Spring Boot → Vue: Result { code:"200", data: InsurPred }
       │
 9. Vue 渲染预测结果卡片: 概率进度条 + 风险等级标签 + 解释因子列表
```

---

## 七、关键调用关系图

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (Vue 3)                         │
│  Login → MotorInsurance → ClaimTypes → VehicleInfo              │
│  PredictionManage → PredictionStatistics                        │
│  ModelTraining → BusinessAnalytics → UserManage                 │
│  utils/request.js (Axios)  ←→  utils/auth.js (sessionStorage)  │
└──────────────────────────┬──────────────────────────────────────┘
                           │ HTTP (withCredentials: true)
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SPRING BOOT (Java)                            │
│                                                                  │
│  Controller (接口层)                                              │
│    AuthController  MotorInsuranceController  ClaimTypesController│
│    VehicleInfoController  InsurPredController                    │
│    ModelTrainingController  AnalyticsController  UserController │
│    FileController                                                │
│       │                                                          │
│       ▼                                                          │
│  Service (业务逻辑层)                                             │
│    InsurPredService ───HTTP──→ FastAPI (/predict)               │
│    ModelTrainingService ─HTTP─→ FastAPI (/training/*)            │
│    UserService  MotorInsuranceService  ClaimTypesService         │
│    VehicleInfoService  AnalyticsService                          │
│       │                                                          │
│       ▼                                                          │
│  Mapper (数据访问层, MyBatis)                                     │
│    UserMapper  MotorInsuranceMapper  ClaimTypesMapper            │
│    VehicleInfoMapper  InsurPredMapper                            │
│       │                                                          │
│       ▼                                                          │
│  Common (基础设施)                                                │
│    Result  SessionUserUtil  RoleEnum  DateUtil  CorsConfig       │
└──────────────────────────┬──────────────────────────────────────┘
                           │ JDBC / MyBatis
                           ▼
                    ┌──────────────┐
                    │   MySQL      │
                    │  gra_data    │
                    └──────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    FASTAPI (Python)                              │
│                                                                  │
│  FastAPIApp.py  ← REST API 入口                                  │
│       │                                                          │
│       ├── InferenceService.py                                    │
│       │     ├── Model.py (InsuranceMLP 网络结构)                  │
│       │     └── DataLoader.py (特征工程 + 预处理)                 │
│       │                                                          │
│       └── TrainingManager.py                                     │
│             ├── TrainConfig.py (超参数配置)                       │
│             ├── TrainModel.py (训练主流程)                        │
│             │     ├── Model.py (模型构建)                         │
│             │     └── DataLoader.py (数据加载 + 特征工程)         │
│             └── DatabaseUtils.py (读取 CSV/MySQL)                │
│                                                                  │
│  文件系统:                                                        │
│    MLP/DataSet/train_data.csv  ←  Spring Boot 导出                │
│    MLP/outputs/                ←  模型 + Scaler + 图表             │
│    MLP/saved_weights/          ←  用户保存的模型版本               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 八、答辩必备知识点

### 8.1 系统能做什么（一句话）

基于历史车险保单数据，使用 MLP 神经网络预测新保单的理赔概率，并提供特征级可解释性分析。

### 8.2 关键数字

| 项目 | 数值 |
|---|---|
| 输入特征维度 | 26 个原始字段（经特征工程后约 30 个数值特征） |
| 网络结构 | 256→512→512→256→128 五层残差 MLP + 32维分类头 |
| 训练/验证/测试比例 | 75% / 15% / 10% |
| 正样本权重 | 3.10（自动或手动调整） |
| 分类阈值 | 自动搜索（默认 F1 最优，min_recall≥0.83） |
| 风险等级 | LOW(<0.30), MEDIUM(0.30~0.60), HIGH(≥0.60) |
| 可解释因子 | TOP3 风险因素 + TOP2 保护因素 |

### 8.3 老师可能问的问题

**Q: 为什么用 MLP 而不用 XGBoost/随机森林？**
A: 论文中有消融实验（`MLP/ablation/` 目录），对比了不同模型结构。MLP 的优势在于可以灵活设计残差连接、可做端到端训练、推理速度快。具体对比数据需要看论文实验结果。

**Q: 特征工程做了什么？**
A: 关键创新是以"最近续保日期"为时间观测点，将所有日期转化为相对时间差（年龄、驾龄、合同持续时长等），将绝对日期转化为有物理意义的数值。其他还包括缺失值中位数填充、99.9%分位数截断、StandardScaler 标准化。

**Q: 可解释性怎么做？**
A: 单特征消融法——逐个将特征替换为训练集中位数，重新预测，计算概率变化 Δ。Δ>0 是风险因素，Δ<0 是保护因素。各取 TOP 返回。

**Q: 前后端怎么通信的？**
A: 前端 Axios 发 HTTP 请求（withCredentials 携带 Cookie），后端 Spring Boot 返回 `Result {code, msg, data}` 统一格式。ML 相关请求由 Spring Boot 用 Hutool HTTP 客户端转发给 Python FastAPI。

**Q: 权限怎么控制的？**
A: 基于 HttpSession。登录后用户对象存 Session，Controller 通过 `SessionUserUtil` 校验。分两级：登录校验（所有接口）+ 管理员校验（训练/用户管理）。数据权限：管理员看全部，普通用户只看自己创建的。

**Q: 为什么 Spring Boot 和 FastAPI 要分开？**
A: Python 生态（PyTorch、sklearn）更适合 ML，Java 生态（Spring Boot、MyBatis）更适合业务 CRUD。分离后各司其职，也能独立扩展。

**Q: 训练数据从哪里来？**
A: 三张业务表（motor_insurance + claim_record + vehicle_info）JOIN 后导入 `train_data` 表，再导出为 CSV 给 FastAPI 读取。

### 8.4 关键类调用速查

| 你想知道... | 看这个文件 |
|---|---|
| 登录校验逻辑 | `UserService.java` → `login()` |
| 权限判断 | `SessionUserUtil.java` |
| 统一返回格式 | `Result.java` |
| 预测怎么调用 ML | `InsurPredService.java` → `callFastApiPredict()` |
| ML 模型结构 | `MLP/Model.py` → `InsuranceMLP` |
| 特征怎么处理 | `MLP/DataLoader.py` → `prepare_features()` |
| 训练怎么跑 | `MLP/TrainModel.py` → `run_training()` |
| 可解释性实现 | `MLP/InferenceService.py` → `_build_local_explanation()` |
| 前端路由和权限 | `vue/src/router/index.js` → `beforeEach` |
| 数据库表结构 | `sql/2026-04-13_claim_record_and_vehicle_info_tables.sql` |
| 训练参数默认值 | `MLP/TrainConfig.py` |
